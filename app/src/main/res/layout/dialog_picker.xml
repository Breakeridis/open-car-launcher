<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_search"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="16dp"
        android:paddingEnd="16dp">

        <ImageView
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:contentDescription="@null"
            android:src="@drawable/ic_search" />

        <EditText
            android:id="@+id/pickerSearch"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:autofillHints=""
            android:background="@null"
            android:hint="@string/search_apps"
            android:imeOptions="actionSearch|flagNoExtractUi"
            android:inputType="text"
            android:maxLines="1"
            android:minHeight="48dp"
            android:singleLine="true"
            android:textColor="@color/cockpit_text_primary"
            android:textColorHint="@color/cockpit_text_dim"
            android:textSize="16sp" />
    </LinearLayout>

    <!-- Fixed height: the dialog root is wrap_content, so a weighted child would collapse.
         280dp fits a 1024x600 head unit with the search field still visible. -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/pickerList"
        android:layout_width="match_parent"
        android:layout_height="280dp"
        android:layout_marginTop="8dp"
        android:scrollbars="vertical" />

</LinearLayout>
