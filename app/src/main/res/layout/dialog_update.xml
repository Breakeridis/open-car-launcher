<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="24dp"
    android:paddingTop="8dp"
    android:paddingEnd="24dp"
    android:paddingBottom="8dp">

    <TextView
        android:id="@+id/updateHeadline"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/cockpit_text_primary"
        android:textSize="18sp"
        tools:text="Version 1.1.0 is available" />

    <!-- Fixed height rather than 0dp+weight: the parent is wrap_content, so a weighted child
         would get zero remaining space and the changelog would be invisible. -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="160dp"
        android:layout_marginTop="12dp">

        <TextView
            android:id="@+id/updateChangelog"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/cockpit_text_secondary"
            android:textSize="14sp"
            tools:text="- Fixed the thing" />
    </ScrollView>

    <ProgressBar
        android:id="@+id/updateProgress"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:max="100"
        android:visibility="gone" />

    <TextView
        android:id="@+id/updateProgressText"
        style="@style/Text.Caption"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:textAllCaps="false"
        android:visibility="gone"
        tools:text="2.1 / 4.7 MB" />

</LinearLayout>
